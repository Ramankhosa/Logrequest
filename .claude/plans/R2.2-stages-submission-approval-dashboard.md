# R2.2 — Stages, Submission, Approval & Dashboard
# ═══════════════════════════════════════════════════════════════════════════════

## Goal

Build the employee-facing achievement workflow: weighted KPI stages with progress
tracking, template-driven submission with contribution roles, approval by
custodian unit, full submission trail, in-app notifications, and an admin
dashboard with org-hierarchy drill-down.

Everything builds on the R2.1 schema foundation. The `KpiStageDefinition` and
`KpiStageProgress` tables already exist. This release adds service logic, new
schema fields, API routes, and UI for the complete stage → submit → approve loop.

---

## Locked Decisions (from brainstorming)

| # | Decision |
|---|---|
| D1 | Stages are **progress milestones**, not approval gates. Employee updates them. |
| D2 | Each stage has a **weight**. Auto-score = sum of completed stage weights. No manual override. |
| D3 | Admin toggles **evidence required per stage** (checkbox). |
| D4 | Admin can set **optional deadline per stage**. |
| D5 | Multi-target KPIs use **tabs (≤5)** or **card list (>5)** — one per achievement instance. |
| D6 | `allowPartialCompletion` flag per KPI. If true → submit anytime. If false → all stages must be done. |
| D7 | **Contribution roles** with credit % defined on KPI (template library deferred to R2.3). |
| D8 | Approving authority = **source division** by default, overridable to another unit via `keyUnitId`/`finalUnitId`. |
| D9 | **Re-submission** allowed while period is IN_PROGRESS. |
| D10 | Full **submission trail** — every submit/reject/approve logged with timestamp, actor, notes. |
| D11 | **In-app notifications** — no email for now. |
| D12 | **No carry-forward**. One achievement claimed once. |
| D13 | Fixed weightages, no self-assessment, no subjective scoring. |
| D14 | KPIs without stages work exactly as R1 — submit button always available. |

---

## PART A: Schema Migration

### A1. Alter `KpiStageDefinition` — add weight, evidence, deadline fields

Current schema has: `id, kpiDefinitionId, stageOrder, title, description, isMandatory, createdAt, updatedAt`

**Add columns:**

```prisma
model KpiStageDefinition {
  // ... existing fields ...
  weight                Float          @default(0)     // contribution to total score
  evidenceRequired      Boolean        @default(false)
  evidenceTypes         EvidenceType[]                  // reuse existing enum
  evidenceInstructions  String?
  deadline              DateTime?                       // optional per-stage deadline
}
```

### A2. Alter `KpiStageProgress` — add achievement link, evidence files

Current schema links to `targetAllocationId`. For multi-target (multiple achievements
per allocation), we need to also link to `achievementId`.

**Add columns:**

```prisma
model KpiStageProgress {
  // ... existing fields ...
  achievementId     String?                            // nullable for backward compat
  notes             String?                            // user notes when marking done
  evidenceFiles     Json?                              // [{name, url, type, uploadedAt}]

  achievement       Achievement?  @relation(fields: [achievementId], references: [id], onDelete: Cascade)
}
```

**Add index:** `@@index([achievementId])`

Update unique constraint: change from `@@unique([targetAllocationId, stageDefinitionId])`
to `@@unique([targetAllocationId, stageDefinitionId, achievementId])` — so each
achievement instance under the same allocation gets its own stage progress set.

### A3. Alter `KpiDefinition` — add partial completion flag and contribution roles

```prisma
model KpiDefinition {
  // ... existing fields ...
  allowPartialCompletion  Boolean    @default(true)
  contributionRoles       Json?      // [{role, creditPercent, isDefault}]
}
```

`contributionRoles` example:
```json
[
  { "role": "First Author",         "creditPercent": 100, "isDefault": false },
  { "role": "Corresponding Author", "creditPercent": 80,  "isDefault": false },
  { "role": "Co-Author",            "creditPercent": 50,  "isDefault": true  },
  { "role": "Supervisor/Mentor",    "creditPercent": 30,  "isDefault": false }
]
```

### A4. Alter `Achievement` — add contribution role and stage score

```prisma
model Achievement {
  // ... existing fields ...
  title                 String?       // user-given name ("IoT Security Paper")
  contributionRole      String?       // "First Author", "Co-Author", etc.
  creditPercent         Float?        // 100, 80, 50, 30 — from role definition
  effectiveScore        Float?        // computedScore * (creditPercent / 100)
  stageCompletionScore  Float?        // sum of completed stage weights (before credit adjustment)
  stageProgress         KpiStageProgress[]  // relation back from KpiStageProgress
}
```

### A5. `Notification` model — already exists

The R2.1 migration already created the `Notification` model. No changes needed.

### A6. New model: `SubmissionTrail`

```prisma
model SubmissionTrail {
  id              String   @id @default(cuid())
  achievementId   String
  action          String   // SUBMITTED, APPROVED, REJECTED, RESUBMITTED, WITHDRAWN
  actorUserId     String
  actorName       String   // denormalized for trail readability
  actorRole       String   // "Employee", "HOD", "IQAC Admin"
  actorUnitName   String?  // department name
  note            String?
  scoreAtAction   Float?   // score at the time of this action
  metadata        Json?    // any extra context
  createdAt       DateTime @default(now())
  achievement     Achievement @relation(fields: [achievementId], references: [id], onDelete: Cascade)

  @@index([achievementId, createdAt])
}
```

Add relation on `Achievement`:
```prisma
  submissionTrail   SubmissionTrail[]
```

### A7. Migration summary

| Change | Type | Table |
|---|---|---|
| Add `weight`, `evidenceRequired`, `evidenceTypes`, `evidenceInstructions`, `deadline` | ALTER | KpiStageDefinition |
| Add `achievementId`, `notes`, `evidenceFiles` | ALTER | KpiStageProgress |
| Change unique constraint to include `achievementId` | ALTER | KpiStageProgress |
| Add `allowPartialCompletion`, `contributionRoles` | ALTER | KpiDefinition |
| Add `title`, `contributionRole`, `creditPercent`, `effectiveScore`, `stageCompletionScore` | ALTER | Achievement |
| Add `SubmissionTrail` model | CREATE | SubmissionTrail |

All changes are non-breaking: nullable columns and new tables only.

---

## PART B: Stage CRUD Service

**File**: `src/lib/kra-kpi/stage-service.ts` (NEW)

### B1. `createStage(kpiId, tenantId, data, actorUserId, actorRole)`

- Permission: admin/owner only
- Validate KPI exists, belongs to tenant, not ARCHIVED
- Validate `stageOrder` is unique for this KPI (or auto-assign next order)
- Validate `weight >= 0`
- Validate evidence types are valid enum values if evidenceRequired = true
- Validate deadline is within the KPI's assessment period date range (if set)
- Create `KpiStageDefinition` record
- Audit log

**Input schema:**
```typescript
{
  title: string;           // required, max 200 chars
  description?: string;    // max 500 chars
  stageOrder?: number;     // auto-assign if omitted
  weight: number;          // >= 0
  isMandatory?: boolean;   // default true
  evidenceRequired?: boolean; // default false
  evidenceTypes?: EvidenceType[];
  evidenceInstructions?: string;
  deadline?: Date;
}
```

### B2. `updateStage(stageId, kpiId, tenantId, data, actorUserId, actorRole)`

- Same permission + validation as create
- Cannot change `stageOrder` directly — use `reorderStages` for that
- Can update: title, description, weight, isMandatory, evidence settings, deadline
- **Locking rule**: if any achievement has stage progress for this stage with
  `isCompleted = true`, warn but allow update (admin responsibility)
- Audit log

### B3. `deleteStage(stageId, kpiId, tenantId, actorUserId, actorRole)`

- Permission: admin/owner
- **Locking rule**: if any KpiStageProgress exists for this stage with
  `isCompleted = true`, reject with error "Stage has completed progress records.
  Delete progress first or archive the stage."
- If no completed progress: cascade delete (KpiStageProgress records auto-deleted)
- Re-order remaining stages to close gaps
- Audit log

### B4. `listStages(kpiId, tenantId)`

- Returns `KpiStageView[]` ordered by `stageOrder`
- Includes aggregate: count of completed progress records per stage (across all allocations)
- No permission restriction — visible to anyone in the tenant

### B5. `reorderStages(kpiId, tenantId, orderedIds[], actorUserId, actorRole)`

- Takes array of stage IDs in desired order
- Updates `stageOrder` for each (1, 2, 3, ...)
- Validate all IDs belong to this KPI
- Audit log

### B6. `getStageWeightSummary(kpiId, tenantId)`

- Returns: `{ totalWeight, stages: [{title, weight, percentage}] }`
- Used by admin UI to show weight distribution
- Example: total = 100, stages = [{Topic: 10, 10%}, {Writing: 25, 25%}, ...]

---

## PART C: Stage Progress Service

**File**: `src/lib/kra-kpi/stage-progress-service.ts` (NEW)

### C1. `initializeStageProgress(achievementId, targetAllocationId, kpiId)`

- Called when employee creates a new achievement instance
- Fetches all `KpiStageDefinition` records for the KPI
- Creates one `KpiStageProgress` record per stage, all `isCompleted = false`
- Links to both `targetAllocationId` and `achievementId`

### C2. `markStageComplete(progressId, tenantId, data, actorUserId)`

- Validate the progress record exists
- Validate the actor is the allocated employee (or unit head with override)
- Validate period is IN_PROGRESS
- If stage has `evidenceRequired = true`:
  - Validate `evidenceFiles` is provided and non-empty
  - Validate file types match `evidenceTypes` on the stage definition
- Set `isCompleted = true`, `completedAt = now()`, `completedByUserId = actorUserId`
- Store `notes` and `evidenceFiles`
- Recalculate `stageCompletionScore` on the Achievement = sum of completed stage weights
- Check if all stages complete → update achievement readiness flag
- Create notification if stage has deadline and was completed late
- Audit log

**Input:**
```typescript
{
  notes?: string;
  evidenceFiles?: Array<{ name: string; url: string; type: string }>;
}
```

### C3. `unmarkStageComplete(progressId, tenantId, actorUserId)`

- Allow employee to undo a stage completion (mistakes happen)
- Only if achievement is still in DRAFT state (not yet submitted)
- Reset `isCompleted = false`, clear `completedAt`, `completedByUserId`
- Recalculate `stageCompletionScore`
- Audit log

### C4. `getStageProgressForAchievement(achievementId, tenantId)`

- Returns `StageProgressView[]` ordered by stage order
- Includes stage definition details (title, weight, evidenceRequired, deadline)
- Includes completion status, timestamp, notes, evidence files
- Includes computed fields:
  - `isOverdue`: deadline < now && !isCompleted
  - `daysRemaining`: deadline - now (null if no deadline)

### C5. `getStageProgressSummary(targetAllocationId, tenantId)`

- For admin/dashboard view
- Groups by achievement instance under this allocation
- Returns per-achievement: `{ achievementId, title, completedStages, totalStages, completionPercent, stageScore, totalWeight }`

### C6. `calculateStageScore(achievementId)`

- Pure calculation: sum of `weight` for all stages where `isCompleted = true`
- Updates `Achievement.stageCompletionScore`
- If contribution role set: `effectiveScore = stageCompletionScore * (creditPercent / 100)`
- Returns `{ stageCompletionScore, effectiveScore }`

---

## PART D: Submission Flow Service

**File**: `src/lib/kra-kpi/achievement-service.ts` (MODIFY existing)

### D1. Modify `createAchievement` — support named instances + contribution role

Current flow: employee creates achievement linked to allocation.

**New behavior:**
- Accept optional `title` field (user names the achievement, e.g. "IoT Security Paper")
- Accept optional `contributionRole` from the KPI's `contributionRoles` list
- If role selected → look up `creditPercent` from KPI definition and store on achievement
- After creating achievement → call `initializeStageProgress()` to set up stage checklist
- If KPI has no stages defined → skip stage initialization (R1 behavior preserved)

### D2. Modify `submitAchievement` — enforce stages + evidence

Current flow: DRAFT → SUBMITTED.

**New validation before submission:**

```
1. If KPI has stages defined:
   a. If allowPartialCompletion = false:
      → ALL stages must be isCompleted = true
      → Error: "Complete all stages before submitting"
   b. If allowPartialCompletion = true:
      → At least 1 stage must be complete (can't submit with 0 progress)
      → Warning only, allow submit

2. If KPI.evidenceRequired = true (KPI-level evidence, separate from stage evidence):
   → Achievement must have evidenceFiles or evidenceLinks
   → Error: "Evidence is required for this KPI"

3. Per-stage evidence:
   → Each completed stage with evidenceRequired = true must have evidenceFiles
   → Already enforced at markStageComplete time, not re-checked here

4. Contribution role:
   → If KPI has contributionRoles defined and achievement.contributionRole is null:
      → Error: "Please select your contribution role"

5. Period must be IN_PROGRESS
   → Error: "Submissions are closed for this period"
```

**After validation passes:**
- Set `state = SUBMITTED`
- Calculate `stageCompletionScore` = sum of completed stage weights
- Calculate `effectiveScore` = stageCompletionScore * (creditPercent / 100)
- Determine approving unit:
  - If `KpiDefinition.finalUnitId` is set → use it (override)
  - Else if `KpiDefinition.keyUnitId` is set → use it
  - Else → use `KpiDefinition.startingUnitId` (source division default)
- Set `currentVerifierUnitId` on achievement
- Find unit head(s) of approving unit → set `currentVerifierUserId`
- Create `SubmissionTrail` record: action = "SUBMITTED"
- Create `Notification` for approver: "Dr. Sharma submitted achievement for Research Publications"
- Audit log

### D3. Modify `recommendAchievement` → rename/repurpose as `approveAchievement`

In R2.2, the 2-level flow (RECOMMEND → VERIFY) simplifies for most cases to:
- Source division approves → VERIFIED

**But** keep backward compatibility: if both `keyUnitId` and `finalUnitId` are set:
- Key unit recommends (SUBMITTED → RECOMMENDED)
- Final unit verifies (RECOMMENDED → VERIFIED)

If only source division:
- Source division approves (SUBMITTED → VERIFIED directly)

### D4. `resubmitAchievement(achievementId, tenantId, actorUserId)`

- Only allowed when `state = REJECTED`
- Only allowed when period is IN_PROGRESS
- Reset `state = DRAFT` (employee can update stages/evidence before resubmitting)
- Create `SubmissionTrail` record: action = "RESUBMITTED"
- Clear `rejectionReason`
- Notification to employee: "You can now update and resubmit"

### D5. Modify `rejectAchievement` — add trail

- Existing rejection logic stays
- **Add**: Create `SubmissionTrail` record: action = "REJECTED", include reason
- **Add**: Notification to employee: "Your achievement was rejected. Reason: ..."

### D6. Score calculation flow

```
Achievement score calculation:

If KPI has stages:
  stageCompletionScore = sum(completed stage weights)

  If contributionRole set:
    effectiveScore = stageCompletionScore × (creditPercent / 100)
  Else:
    effectiveScore = stageCompletionScore

If KPI has NO stages:
  Use existing R1 computeScore() based on actual vs target values

  If contributionRole set:
    effectiveScore = computedScore × (creditPercent / 100)
  Else:
    effectiveScore = computedScore

The effectiveScore is what appears on the employee's scorecard.
```

---

## PART E: Notification Service

**File**: `src/lib/notification-service.ts` (NEW)

### E1. `createNotification(tenantId, userId, data)`

```typescript
{
  type: string;         // see list below
  title: string;        // short, for notification list
  message: string;      // detailed
  entityType?: string;  // "Achievement", "TargetAllocation", "KpiStageProgress"
  entityId?: string;
  linkUrl?: string;     // deep link to relevant page
}
```

### E2. `getNotifications(tenantId, userId, options?)`

- Returns notifications for user, ordered by `createdAt DESC`
- Options: `{ unreadOnly?: boolean, limit?: number, offset?: number }`
- Returns `{ notifications: NotificationView[], unreadCount: number }`

### E3. `markAsRead(notificationId, tenantId, userId)`

- Sets `isRead = true`

### E4. `markAllAsRead(tenantId, userId)`

- Bulk update all unread notifications for this user

### E5. `getUnreadCount(tenantId, userId)`

- Returns count of unread notifications
- Used by the notification bell icon in the header

### E6. Notification types

| Type | Trigger | Recipient |
|---|---|---|
| `ACHIEVEMENT_SUBMITTED` | Employee submits | Approving unit head(s) |
| `ACHIEVEMENT_APPROVED` | Approver approves | Employee |
| `ACHIEVEMENT_REJECTED` | Approver rejects | Employee |
| `ACHIEVEMENT_RESUBMITTED` | Employee resubmits | Approving unit head(s) |
| `STAGE_DEADLINE_APPROACHING` | 3 days before stage deadline | Employee |
| `STAGE_DEADLINE_OVERDUE` | Stage deadline passed, not complete | Employee |
| `ACHIEVEMENT_DEADLINE_APPROACHING` | 7 days before period achievement deadline | Employee |
| `KPI_ALLOCATED` | New allocation created for user/unit | Assigned employee/unit head |
| `REVIEW_PENDING` | Submission waiting > 7 days | Approving unit head(s) |

---

## PART F: Contribution Roles Service

**File**: `src/lib/kra-kpi/kpi-service.ts` (MODIFY existing)

### F1. Extend `createKpi` and `updateKpi` — accept `contributionRoles`

**Validation:**
```typescript
const contributionRoleSchema = z.object({
  role: z.string().trim().min(1).max(100),
  creditPercent: z.number().min(1).max(100),
  isDefault: z.boolean().default(false),
});

// Validate:
// - At most one role can be isDefault = true
// - No duplicate role names
// - creditPercent between 1 and 100
```

### F2. Extend `createKpi` and `updateKpi` — accept `allowPartialCompletion`

Simple boolean, default true.

### F3. At achievement submission — validate contribution role

- If KPI has `contributionRoles` (non-null, non-empty array):
  - Achievement must have `contributionRole` set
  - Role must match one of the defined roles
  - `creditPercent` is looked up from the matching role (not user-supplied)

---

## PART G: Admin Dashboard Service

**File**: `src/lib/kra-kpi/dashboard-service.ts` (NEW)

### G1. `getOverviewStats(tenantId, periodId)`

Returns:
```typescript
{
  totalKpis: number;
  totalAllocations: number;
  totalAchievements: number;
  achievementsByState: {
    DRAFT: number;
    SUBMITTED: number;
    RECOMMENDED: number;
    VERIFIED: number;
    REJECTED: number;
  };
  overallCompletionPercent: number;  // verified / total allocations
  overdueCount: number;             // past achievement deadline, not submitted
  pendingReviewCount: number;       // SUBMITTED but not yet approved/rejected
}
```

### G2. `getOrgHierarchyStats(tenantId, periodId, parentUnitId?)`

Drill-down by org hierarchy. If no `parentUnitId`, returns top-level (schools).
If `parentUnitId` = a school, returns departments under it.

Returns:
```typescript
{
  units: Array<{
    unitId: string;
    unitName: string;
    unitCode: string;
    category: OrgUnitCategory;
    totalAllocations: number;
    completedAllocations: number;
    completionPercent: number;
    averageScore: number;
    childUnitCount: number;  // for drill-down indicator
  }>;
}
```

### G3. `getKpiCrossComparison(tenantId, periodId, kpiId)`

Compare a single KPI across all assigned departments.

Returns:
```typescript
{
  kpiTitle: string;
  targetTotal: number;
  achievedTotal: number;
  units: Array<{
    unitId: string;
    unitName: string;
    target: number;
    achieved: number;
    completionPercent: number;
    achievementCount: number;
  }>;
}
```

### G4. `getAttentionItems(tenantId, periodId)`

Things that need admin attention.

Returns:
```typescript
{
  overdueAchievements: number;         // past deadline, not submitted
  zeroProgressEmployees: number;       // no stage progress at all
  stalePendingReviews: number;         // SUBMITTED > 7 days, not reviewed
  lowCompletionKpis: Array<{           // KPIs with < 50% completion
    kpiId: string;
    kpiTitle: string;
    completionPercent: number;
  }>;
}
```

### G5. `getStageBottleneckAnalysis(tenantId, periodId, kpiId)`

Where are people getting stuck?

Returns:
```typescript
{
  kpiTitle: string;
  stages: Array<{
    stageOrder: number;
    title: string;
    totalAssigned: number;
    completedCount: number;
    completionPercent: number;
    averageDaysToComplete: number | null;
  }>;
}
```

### G6. `getPersonDetail(tenantId, periodId, userId)`

Single person's complete KPI picture.

Returns:
```typescript
{
  userId: string;
  userName: string;
  unitName: string;
  allocations: Array<{
    kpiTitle: string;
    target: string;
    stagesComplete: number;
    stagesTotal: number;
    completionPercent: number;
    score: number;
    state: AchievementState;
    isOverdue: boolean;
  }>;
  overallScore: number;
  overallCompletion: number;
}
```

---

## PART H: API Routes

### H1. Stage CRUD routes

**`/api/tenant/kra-kpi/kpis/[id]/stages/route.ts`** (NEW)

| Method | Action | Status Codes |
|---|---|---|
| GET | `listStages` | 200, 404 |
| POST | `createStage` | 201, 400, 403, 404 |

**`/api/tenant/kra-kpi/kpis/[id]/stages/[stageId]/route.ts`** (NEW)

| Method | Action | Status Codes |
|---|---|---|
| PATCH | `updateStage` | 200, 400, 403, 404 |
| DELETE | `deleteStage` | 200, 403, 404, 409 |

**`/api/tenant/kra-kpi/kpis/[id]/stages/reorder/route.ts`** (NEW)

| Method | Action | Status Codes |
|---|---|---|
| POST | `reorderStages` | 200, 400, 403, 404 |

### H2. Stage progress routes

**`/api/tenant/kra-kpi/achievements/[id]/stage-progress/route.ts`** (NEW)

| Method | Action | Status Codes |
|---|---|---|
| GET | `getStageProgressForAchievement` | 200, 404 |

**`/api/tenant/kra-kpi/stage-progress/[id]/complete/route.ts`** (NEW)

| Method | Action | Status Codes |
|---|---|---|
| POST | `markStageComplete` | 200, 400, 403, 404 |

**`/api/tenant/kra-kpi/stage-progress/[id]/uncomplete/route.ts`** (NEW)

| Method | Action | Status Codes |
|---|---|---|
| POST | `unmarkStageComplete` | 200, 400, 403, 404 |

### H3. Submission flow routes

**`/api/tenant/kra-kpi/achievements/[id]/submit/route.ts`** (MODIFY)

- Enhance existing submit endpoint with stage validation, contribution role validation

**`/api/tenant/kra-kpi/achievements/[id]/resubmit/route.ts`** (NEW)

| Method | Action | Status Codes |
|---|---|---|
| POST | `resubmitAchievement` | 200, 400, 403, 404 |

**`/api/tenant/kra-kpi/achievements/[id]/trail/route.ts`** (NEW)

| Method | Action | Status Codes |
|---|---|---|
| GET | `getSubmissionTrail` | 200, 404 |

### H4. Notification routes

**`/api/tenant/notifications/route.ts`** (NEW)

| Method | Action | Status Codes |
|---|---|---|
| GET | `getNotifications` (query: unreadOnly, limit, offset) | 200 |

**`/api/tenant/notifications/[id]/read/route.ts`** (NEW)

| Method | Action | Status Codes |
|---|---|---|
| POST | `markAsRead` | 200, 404 |

**`/api/tenant/notifications/read-all/route.ts`** (NEW)

| Method | Action | Status Codes |
|---|---|---|
| POST | `markAllAsRead` | 200 |

**`/api/tenant/notifications/count/route.ts`** (NEW)

| Method | Action | Status Codes |
|---|---|---|
| GET | `getUnreadCount` | 200 |

### H5. Dashboard routes

**`/api/tenant/kra-kpi/dashboard/overview/route.ts`** (NEW)

| Method | Action | Query Params |
|---|---|---|
| GET | `getOverviewStats` | periodId |

**`/api/tenant/kra-kpi/dashboard/org-hierarchy/route.ts`** (NEW)

| Method | Action | Query Params |
|---|---|---|
| GET | `getOrgHierarchyStats` | periodId, parentUnitId? |

**`/api/tenant/kra-kpi/dashboard/kpi-comparison/route.ts`** (NEW)

| Method | Action | Query Params |
|---|---|---|
| GET | `getKpiCrossComparison` | periodId, kpiId |

**`/api/tenant/kra-kpi/dashboard/attention/route.ts`** (NEW)

| Method | Action | Query Params |
|---|---|---|
| GET | `getAttentionItems` | periodId |

**`/api/tenant/kra-kpi/dashboard/stage-bottleneck/route.ts`** (NEW)

| Method | Action | Query Params |
|---|---|---|
| GET | `getStageBottleneckAnalysis` | periodId, kpiId |

**`/api/tenant/kra-kpi/dashboard/person/route.ts`** (NEW)

| Method | Action | Query Params |
|---|---|---|
| GET | `getPersonDetail` | periodId, userId |

---

## PART I: UI Components

### I1. Admin — Stage Manager (KPI detail page)

**File**: `src/components/tenant/kra-kpi/kpi-stage-manager.tsx` (NEW)

```
KPI: "Research Publications"

Stages                        Total Weight: 100
┌────┬─────────────────────┬────────┬──────────┬────────────┬──────────┐
│ #  │ Title               │ Weight │ Evidence │ Deadline   │ Actions  │
├────┼─────────────────────┼────────┼──────────┼────────────┼──────────┤
│ 1  │ Topic Selection     │ 10     │ ☐ No     │ —          │ ✏️ 🗑️ ↕  │
│ 2  │ Paper Writing       │ 25     │ ☐ No     │ Jun 30     │ ✏️ 🗑️ ↕  │
│ 3  │ Journal Submission  │ 20     │ ☑ Yes    │ Aug 31     │ ✏️ 🗑️ ↕  │
│ 4  │ Peer Review         │ 20     │ ☑ Yes    │ —          │ ✏️ 🗑️ ↕  │
│ 5  │ Published           │ 25     │ ☑ Yes    │ —          │ ✏️ 🗑️ ↕  │
└────┴─────────────────────┴────────┴──────────┴────────────┴──────────┘
  [+ Add Stage]

Contribution Roles
┌─────────────────────┬───────────┬─────────┐
│ Role                │ Credit %  │ Default │
├─────────────────────┼───────────┼─────────┤
│ First Author        │ 100%      │ ☐       │
│ Corresponding Author│ 80%       │ ☐       │
│ Co-Author           │ 50%       │ ☑       │
│ Supervisor/Mentor   │ 30%       │ ☐       │
└─────────────────────┴───────────┴─────────┘
  [+ Add Role]

Settings
  ☑ Allow Partial Completion
```

- Drag handle (↕) for reordering stages
- Inline edit or modal for each stage
- Weight total shown with warning if doesn't equal KPI weightage
- Evidence toggle per stage with expandable evidence type selector

### I2. Employee — Achievement Tabs/Cards with Stage Progress

**File**: `src/components/my-kpis/my-achievement-stages.tsx` (NEW)

**For target ≤ 5 — Tab UI:**
```
Research Publications (Target: 3)    Progress: 1.5/3.0 papers

┌──────────────┐┌─────────────────┐┌───────────┐
│ IoT Security ││ ML Healthcare   ││ + Add New  │
│ 🔄 3/5       ││ ✅ 5/5          ││           │
└──────────────┘└─────────────────┘└───────────┘
┌─────────────────────────────────────────────────┐
│  Title: "IoT Security Framework"          [✏️]  │
│  Role: Co-Author (50% credit)                   │
│                                                  │
│  ✅ Topic Selection         Jan 15               │
│     "Selected IoT security topic"                │
│                                                  │
│  ✅ Paper Writing           Feb 28               │
│     "Draft complete, 12 pages"                   │
│                                                  │
│  ✅ Journal Submission      Mar 5                │
│     📎 submission_receipt.pdf                    │
│     "Submitted to IEEE Access"                   │
│                                                  │
│  🔄 Peer Review            [Mark Complete]       │
│     ⚠️ No deadline set                          │
│                                                  │
│  ⬜ Published                                    │
│                                                  │
│  Score: 55/100 (× 50% credit = 27.5 effective)  │
│                                                  │
│  🔒 Submit Achievement                           │
│     (complete all stages first)                  │
│     — OR if partial allowed —                    │
│  🟢 Submit Achievement (partial: 55/100)         │
└─────────────────────────────────────────────────┘
```

**For target > 5 — Card list UI:**
```
Research Publications (Target: 20)    [+ Add New]

Progress: 5.5/20.0 papers

┌─────────────────────────────────┬────────────┬─────────┐
│ IoT Security Framework          │ 3/5 ████░░ │ 🔄      │
│ Co-Author (50%)                 │ Score: 27.5│         │
├─────────────────────────────────┼────────────┼─────────┤
│ ML in Healthcare                │ 5/5 █████  │ ✅ Done │
│ First Author (100%)             │ Score: 100 │         │
├─────────────────────────────────┼────────────┼─────────┤
│ Blockchain Education            │ 1/5 █░░░░  │ 🔄      │
│ Co-Author (50%)                 │ Score: 5   │         │
├─────────────────────────────────┼────────────┼─────────┤
│ + 17 more to add...            │            │         │
└─────────────────────────────────┴────────────┴─────────┘
```

Click card → expands to show full stage checklist.

### I3. Employee — Submission Form

**File**: `src/components/my-kpis/my-achievement-submit.tsx` (NEW)

Shown when employee clicks "Submit Achievement":

```
┌─────────────────────────────────────────────────────┐
│  Submit: "IoT Security Framework"                    │
│                                                      │
│  ── From KPI Template ────────────────────────────   │
│  KPI: Research Publications                          │
│  Measurement: Numeric (papers)                       │
│  Your Target: 1 paper                                │
│  Your Role: Co-Author (50% credit)                   │
│                                                      │
│  ── Stage Summary (auto) ─────────────────────────   │
│  ✅ Topic Selection (10 pts)      Jan 15             │
│  ✅ Paper Writing (25 pts)        Feb 28             │
│  ✅ Journal Submission (20 pts)   Mar 5              │
│  🔄 Peer Review (20 pts)         — incomplete        │
│  ⬜ Published (25 pts)           — incomplete         │
│  Stage Score: 55/100                                 │
│  Effective Score: 27.5 (55 × 50%)                    │
│                                                      │
│  ── Achievement Template Fields ──────────────────   │
│  (auto-populated from KPI's achievementFormConfig)   │
│  Paper Title: [IoT Security Framework          ]     │
│  Journal: [IEEE Access                         ]     │
│  ISSN: [2169-3536                              ]     │
│  DOI: [                                        ]     │
│  Publication Date: [                           ]     │
│  Co-Authors: [Dr. Patel, Dr. Kumar             ]     │
│                                                      │
│  ── KPI-Level Evidence ───────────────────────────   │
│  ⚠️ Evidence required (Document, URL)                │
│  [📄 Upload PDF]  paper.pdf ✅                       │
│  [🔗 Add URL]    https://doi.org/... ✅              │
│                                                      │
│  ── Notes ────────────────────────────────────────   │
│  [Optional notes for the reviewer...           ]     │
│                                                      │
│              [Cancel]  [Submit for Review]            │
└─────────────────────────────────────────────────────┘
```

### I4. Approver — Review Queue Enhancement

**File**: `src/components/my-kpis/my-review-queue.tsx` (MODIFY existing)

Add to existing review queue:
- Stage progress summary per achievement (3/5 stages, 55/100 score)
- Submission trail (expandable history)
- Contribution role badge ("Co-Author 50%")
- Stage evidence viewer (click to see evidence per stage)

```
Review Queue (5 pending)

┌─────────────────────────────────────────────────────┐
│ Dr. Sharma — "IoT Security Framework"               │
│ KPI: Research Publications │ Co-Author (50%)         │
│ Stages: 3/5 │ Score: 27.5 │ Submitted: Mar 20       │
│                                                      │
│ [View Details]  [Approve ✅]  [Reject ❌]            │
│                                                      │
│ ▸ Submission Trail (3 entries)                       │
│   Mar 20 — Submitted by Dr. Sharma (55/100)         │
│   Mar 22 — Rejected by Dr. Patel "Need receipt"     │
│   Mar 23 — Resubmitted by Dr. Sharma                │
└─────────────────────────────────────────────────────┘
```

### I5. Notification Bell

**File**: `src/components/layout/notification-bell.tsx` (NEW)

- In the app header/navbar
- Shows unread count badge
- Click opens dropdown with recent notifications
- Each notification links to the relevant page
- "Mark all as read" button

### I6. Admin Dashboard Page

**File**: `src/app/tenant-admin/kra-kpi/dashboard/page.tsx` (NEW)

Full dashboard with:
- Overview cards (total KPIs, allocations, completion %, pending reviews)
- Org hierarchy table with drill-down (click school → see departments)
- KPI cross-comparison chart (select a KPI, see dept-by-dept progress bars)
- Attention items panel (overdue, zero progress, stale reviews)
- Stage bottleneck chart (select a KPI, see where people are stuck)
- Person detail modal (click a person → see their full KPI picture)
- Period selector dropdown
- Filters: by KRA category, by department, by KPI

---

## PART J: Tests

### J1. Schema migration tests (~10 tests)

```
- New columns exist on KpiStageDefinition (weight, evidenceRequired, etc.)
- New columns exist on KpiStageProgress (achievementId, notes, evidenceFiles)
- New columns exist on KpiDefinition (allowPartialCompletion, contributionRoles)
- New columns exist on Achievement (title, contributionRole, creditPercent, effectiveScore, stageCompletionScore)
- SubmissionTrail model exists and can be queried
- Default values correct (allowPartialCompletion = true, weight = 0, evidenceRequired = false)
- Cascade delete: delete Achievement → SubmissionTrail deleted
- Cascade delete: delete Achievement → KpiStageProgress deleted
- Updated unique constraint on KpiStageProgress works with achievementId
```

### J2. Stage CRUD tests (~15 tests)

```
- Create stage: success with all fields
- Create stage: auto-assigns stageOrder if omitted
- Create stage: rejects if KPI archived
- Create stage: non-admin gets 403
- Create stage: duplicate stageOrder rejected
- Update stage: weight, evidence settings, deadline
- Delete stage: success when no completed progress
- Delete stage: blocked when completed progress exists
- List stages: ordered by stageOrder
- List stages: empty for KPI with no stages
- Reorder stages: success, stageOrder updated correctly
- Reorder stages: rejects if IDs don't match KPI
- Weight summary: correct totals
- Cross-tenant: cannot create stage on other tenant's KPI
- Evidence types validation: rejects invalid enum values
```

### J3. Stage progress tests (~20 tests)

```
- Initialize progress: creates N records for N stages
- Initialize progress: skipped for KPIs with no stages
- Mark complete: success, sets completedAt and completedByUserId
- Mark complete: requires evidence when stage.evidenceRequired = true
- Mark complete: rejects when evidence not provided
- Mark complete: rejects when period is not IN_PROGRESS
- Mark complete: only assigned employee can mark (not random user)
- Unmark complete: success when achievement is DRAFT
- Unmark complete: rejected when achievement is SUBMITTED
- Calculate stage score: correct sum of completed weights
- Calculate stage score: zero when nothing complete
- Calculate effective score: stageScore × creditPercent
- Progress per achievement: separate stage sets for same allocation, different achievements
- Progress summary: correct aggregation across multiple achievements
- Stage deadline: isOverdue correctly calculated
- Stage deadline: daysRemaining correctly calculated
- Cross-tenant isolation
- Concurrent mark complete: two users marking same stage (idempotent or conflict)
- Score recalculation after unmark
- Weight = 0 stage: doesn't affect score but still trackable
```

### J4. Submission flow tests (~20 tests)

```
- Submit: success when all stages complete
- Submit: success when partial + allowPartialCompletion = true
- Submit: rejected when partial + allowPartialCompletion = false
- Submit: rejected when 0 stages complete (even with partial allowed)
- Submit: rejected when contribution role missing but KPI has roles defined
- Submit: rejected when KPI evidence required but not provided
- Submit: rejected when period not IN_PROGRESS
- Submit: sets currentVerifierUnitId correctly (source division default)
- Submit: sets currentVerifierUnitId correctly (override via finalUnitId)
- Submit: creates SubmissionTrail record
- Submit: creates notification for approver
- Submit: calculates effectiveScore correctly
- Approve: state transitions to VERIFIED
- Approve: creates trail and notification
- Reject: state transitions to REJECTED, reason stored
- Reject: creates trail and notification
- Resubmit: only from REJECTED state
- Resubmit: resets to DRAFT, creates trail
- Resubmit: rejected when period not IN_PROGRESS
- Submission trail: returns ordered history
```

### J5. Contribution role tests (~10 tests)

```
- Create KPI with contribution roles: stored correctly
- Update KPI: add/remove/modify roles
- Validate: no duplicate role names
- Validate: at most one isDefault
- Validate: creditPercent 1-100
- Achievement: role selection populates creditPercent from KPI definition
- Achievement: effectiveScore = computedScore × creditPercent / 100
- Achievement: no role required when KPI has no roles defined
- Achievement: reject invalid role name
- Score: credit applied correctly at 100%, 80%, 50%, 30%
```

### J6. Notification tests (~10 tests)

```
- Create notification: stored correctly
- List notifications: ordered by createdAt desc
- List notifications: unread filter works
- List notifications: pagination (limit + offset)
- Mark as read: isRead updated
- Mark all as read: all unread → read
- Unread count: correct count
- Cross-tenant isolation: can't see other tenant's notifications
- Notification created on achievement submit
- Notification created on achievement approve/reject
```

### J7. Dashboard tests (~15 tests)

```
- Overview stats: correct counts by achievement state
- Overview stats: correct completion percent
- Org hierarchy: returns top-level units with correct stats
- Org hierarchy: drill-down returns child units
- Org hierarchy: empty for period with no allocations
- KPI cross-comparison: correct per-unit breakdown
- KPI cross-comparison: handles units with zero achievements
- Attention items: identifies overdue achievements
- Attention items: identifies zero-progress employees
- Attention items: identifies stale pending reviews (> 7 days)
- Stage bottleneck: correct per-stage completion counts
- Stage bottleneck: handles KPI with no stages (empty result)
- Person detail: returns full allocation list with scores
- Cross-tenant isolation on all dashboard endpoints
- Dashboard with no data: returns zeros, not errors
```

### J8. API route tests (~20 tests)

```
- Stage routes: GET/POST/PATCH/DELETE with correct status codes
- Stage progress routes: POST complete/uncomplete
- Submission routes: POST submit/resubmit
- Trail route: GET returns history
- Notification routes: GET list, POST read, POST read-all, GET count
- Dashboard routes: all 6 endpoints return correct shape
- Permission matrix: admin-only routes return 403 for non-admin
- All routes: 404 for non-existent resources
- All routes: cross-tenant returns 404
```

### Total test count: ~120 new tests

---

## PART K: Implementation Order

Execute in this order to minimize blocked dependencies:

| Step | What | Depends On | Estimated Effort |
|---|---|---|---|
| K1 | Schema migration (Part A) | Nothing | Small |
| K2 | Notification service (Part E) | K1 | Small |
| K3 | Stage CRUD service (Part B) | K1 | Medium |
| K4 | Stage progress service (Part C) | K1, K3 | Medium |
| K5 | Contribution roles in KPI service (Part F) | K1 | Small |
| K6 | Submission flow modifications (Part D) | K1, K2, K4, K5 | Large |
| K7 | API routes (Part H) | K2, K3, K4, K5, K6 | Medium |
| K8 | Dashboard service (Part G) | K1, K6 | Medium |
| K9 | Admin UI — Stage Manager (I1) | K7 | Medium |
| K10 | Employee UI — Tabs/Cards + Stages (I2) | K7 | Large |
| K11 | Employee UI — Submission Form (I3) | K7, K10 | Medium |
| K12 | Approver UI — Review Queue Enhancement (I4) | K7 | Medium |
| K13 | Notification Bell UI (I5) | K7 | Small |
| K14 | Admin Dashboard UI (I6) | K8 | Large |
| K15 | Tests (Part J) | K2-K8 | Large |

**Critical path**: K1 → K3 → K4 → K6 → K7 → K10 → K11

---

## PART L: Files Changed/Created Summary

### New Files
```
src/lib/kra-kpi/stage-service.ts
src/lib/kra-kpi/stage-progress-service.ts
src/lib/kra-kpi/dashboard-service.ts
src/lib/notification-service.ts
src/app/api/tenant/kra-kpi/kpis/[id]/stages/route.ts
src/app/api/tenant/kra-kpi/kpis/[id]/stages/[stageId]/route.ts
src/app/api/tenant/kra-kpi/kpis/[id]/stages/reorder/route.ts
src/app/api/tenant/kra-kpi/achievements/[id]/stage-progress/route.ts
src/app/api/tenant/kra-kpi/achievements/[id]/trail/route.ts
src/app/api/tenant/kra-kpi/achievements/[id]/resubmit/route.ts
src/app/api/tenant/kra-kpi/stage-progress/[id]/complete/route.ts
src/app/api/tenant/kra-kpi/stage-progress/[id]/uncomplete/route.ts
src/app/api/tenant/notifications/route.ts
src/app/api/tenant/notifications/[id]/read/route.ts
src/app/api/tenant/notifications/read-all/route.ts
src/app/api/tenant/notifications/count/route.ts
src/app/api/tenant/kra-kpi/dashboard/overview/route.ts
src/app/api/tenant/kra-kpi/dashboard/org-hierarchy/route.ts
src/app/api/tenant/kra-kpi/dashboard/kpi-comparison/route.ts
src/app/api/tenant/kra-kpi/dashboard/attention/route.ts
src/app/api/tenant/kra-kpi/dashboard/stage-bottleneck/route.ts
src/app/api/tenant/kra-kpi/dashboard/person/route.ts
src/components/tenant/kra-kpi/kpi-stage-manager.tsx
src/components/my-kpis/my-achievement-stages.tsx
src/components/my-kpis/my-achievement-submit.tsx
src/components/layout/notification-bell.tsx
src/app/tenant-admin/kra-kpi/dashboard/page.tsx
prisma/migrations/YYYYMMDD_r2_2_stages_submission/migration.sql
tests/kra-kpi/r2-2-stages.test.ts
tests/kra-kpi/r2-2-submission.test.ts
tests/kra-kpi/r2-2-notifications.test.ts
tests/kra-kpi/r2-2-dashboard.test.ts
tests/kra-kpi/r2-2-api.route.test.ts
```

### Modified Files
```
prisma/schema.prisma
src/lib/kra-kpi/shared.ts                    — new view types
src/lib/kra-kpi/kpi-service.ts               — contribution roles, allowPartialCompletion
src/lib/kra-kpi/achievement-service.ts       — submission flow, stage validation
src/lib/kra-kpi/my-kpi-service.ts            — stage progress in my allocations
src/components/my-kpis/my-allocation-card.tsx — stage progress display
src/components/my-kpis/my-review-queue.tsx    — trail + stage summary
src/components/tenant/kra-kpi/kpi-definition-form.tsx — stages section, roles section
src/app/tenant-admin/kra-kpi/kra-kpi-hub.tsx — dashboard link
```

---

## Assumptions

1. File upload infrastructure (S3, local storage) does NOT exist yet. Evidence files
   in R2.2 are **URL references only** (user pastes a link or provides a URL to an
   externally hosted file). Actual file upload with binary storage is deferred to R2.3.

2. The Notification model already exists in the schema (added in R2.1 migration cycle).
   No migration needed for it.

3. Existing 2-level verification (RECOMMEND → VERIFY) is preserved. R2.2 adds
   submission trail and stage-aware scoring on top of it, not replacing it.

4. Stage weights are absolute values (not percentages). The admin is responsible for
   making them sum to a meaningful total. The system shows the total but does not
   enforce it equals the KPI weightage.

5. Contribution role credit percentages reduce the effective score but do NOT change
   the target value. A co-author with 50% credit on a target of 3 papers still needs
   to submit 3 achievement instances, but each counts as 0.5 toward their scorecard.

6. Dashboard queries use direct Prisma aggregations. No materialized views or
   background jobs in this release. If performance is an issue with large datasets,
   caching/materialization moves to R2.3.

---

## PART M: Exhaustive Test Scenarios (for Codex)

> These are manual/automated test scenarios covering every dependency, state
> transition, validation, flow, and edge case across all R2.2 features.
> Each scenario includes **preconditions**, **action**, and **expected result**.

---

### M1. Schema & Migration Tests

```
M1.1  KpiStageDefinition new columns exist
      Pre: Run migration
      Act: prisma.kpiStageDefinition.create({ weight: 10, evidenceRequired: true, evidenceTypes: ["DOCUMENT"], deadline: new Date() })
      Exp: Record created with all fields populated

M1.2  KpiStageDefinition defaults
      Pre: Create a stage with only required fields (title, stageOrder, kpiDefinitionId)
      Exp: weight=0, evidenceRequired=false, evidenceTypes=[], deadline=null, isMandatory=true

M1.3  KpiStageProgress new columns exist
      Pre: Create stage progress with achievementId, notes, evidenceFiles
      Exp: All fields stored correctly

M1.4  KpiStageProgress unique constraint updated
      Pre: Same targetAllocationId + stageDefinitionId but DIFFERENT achievementId
      Exp: Both records created (no unique violation)
      Pre2: Same targetAllocationId + stageDefinitionId + SAME achievementId
      Exp2: Unique constraint violation

M1.5  KpiDefinition new columns
      Pre: Update KPI with allowPartialCompletion=false, contributionRoles=[{role:"Author",creditPercent:100,isDefault:true}]
      Exp: Fields stored correctly, retrievable

M1.6  Achievement new columns
      Pre: Create achievement with title, contributionRole, creditPercent, effectiveScore, stageCompletionScore
      Exp: All fields stored and retrievable

M1.7  SubmissionTrail model
      Pre: Create SubmissionTrail record
      Exp: Record created with id, achievementId, action, actorUserId, actorName, actorRole, note, scoreAtAction, metadata, createdAt

M1.8  Cascade delete: Achievement → SubmissionTrail
      Pre: Achievement with 3 SubmissionTrail records
      Act: Delete achievement
      Exp: All 3 trail records deleted

M1.9  Cascade delete: Achievement → KpiStageProgress
      Pre: Achievement with 5 stage progress records
      Act: Delete achievement
      Exp: All 5 progress records deleted

M1.10 allowPartialCompletion default
      Pre: Create KPI without specifying allowPartialCompletion
      Exp: Defaults to true
```

---

### M2. Stage CRUD Service Tests

```
M2.1  createStage — happy path
      Pre: Active KPI exists, user is TENANT_ADMIN
      Act: createStage(kpiId, tenantId, { title: "Topic Selection", weight: 10 }, userId, "TENANT_ADMIN")
      Exp: { ok: true, data: { id: "..." } }

M2.2  createStage — auto-assigns stageOrder
      Pre: KPI with 2 existing stages (order 1, 2)
      Act: createStage without stageOrder
      Exp: New stage gets stageOrder=3

M2.3  createStage — explicit stageOrder
      Pre: KPI with stage at order 1
      Act: createStage with stageOrder=2
      Exp: Success, stage created at order 2

M2.4  createStage — duplicate stageOrder rejected
      Pre: KPI with stage at order 1
      Act: createStage with stageOrder=1
      Exp: { ok: false, message: "Stage order 1 is already used..." }

M2.5  createStage — archived KPI rejected
      Pre: KPI in ARCHIVED state
      Act: createStage
      Exp: { ok: false, message: "Cannot add stages to an archived KPI." }

M2.6  createStage — non-admin rejected
      Pre: User is TENANT_USER
      Act: createStage
      Exp: { ok: false, code: "FORBIDDEN" }

M2.7  createStage — evidenceRequired without evidenceTypes rejected
      Pre: Admin user
      Act: createStage({ evidenceRequired: true, evidenceTypes: [] })
      Exp: { ok: false, message: "Select at least one evidence type..." }

M2.8  createStage — evidenceRequired with valid types
      Act: createStage({ evidenceRequired: true, evidenceTypes: ["DOCUMENT", "URL"] })
      Exp: Success

M2.9  createStage — deadline outside period rejected
      Pre: Period is Jan 1 - Dec 31 2026
      Act: createStage({ deadline: "2027-03-01" })
      Exp: { ok: false, message: "Stage deadline must fall within the assessment period." }

M2.10 createStage — deadline within period accepted
      Act: createStage({ deadline: "2026-06-15" })
      Exp: Success

M2.11 createStage — audit log created
      Pre: Successful stage creation
      Exp: AuditLog record with targetType="KpiStageDefinition", action="CREATE"

M2.12 updateStage — all fields
      Pre: Existing stage
      Act: updateStage({ title: "New Title", weight: 20, evidenceRequired: true, evidenceTypes: ["URL"], deadline: "2026-08-01" })
      Exp: All fields updated

M2.13 updateStage — partial update
      Act: updateStage({ weight: 15 }) — only weight
      Exp: Only weight changed, other fields unchanged

M2.14 updateStage — archived KPI rejected
      Exp: { ok: false, message: "Cannot edit stages on an archived KPI." }

M2.15 updateStage — non-admin rejected
      Exp: { ok: false, code: "FORBIDDEN" }

M2.16 updateStage — cross-tenant rejected
      Pre: Stage belongs to tenant A, user is admin of tenant B
      Exp: { ok: false, message: "Stage not found." }

M2.17 deleteStage — success when no completed progress
      Pre: Stage with 0 completed progress records (some uncompleted OK)
      Act: deleteStage
      Exp: { ok: true }, stage deleted, remaining stages reordered

M2.18 deleteStage — blocked when completed progress exists
      Pre: Stage with 1+ completed progress records
      Act: deleteStage
      Exp: { ok: false, code: "CONFLICT" }

M2.19 deleteStage — reorders remaining stages
      Pre: Stages at order 1, 2, 3. Delete stage at order 2
      Exp: Remaining stages reordered to 1, 2

M2.20 deleteStage — cascade deletes uncompleted progress
      Pre: Stage with 3 uncompleted progress records
      Act: deleteStage
      Exp: All 3 progress records deleted

M2.21 listStages — ordered by stageOrder
      Pre: Stages at order 3, 1, 2
      Exp: Returns in order 1, 2, 3

M2.22 listStages — includes completedProgressCount
      Pre: Stage with 5 completed progress records
      Exp: stage.completedProgressCount === 5

M2.23 listStages — empty for KPI with no stages
      Exp: Returns []

M2.24 listStages — cross-tenant returns empty
      Pre: KPI belongs to tenant A
      Act: listStages(kpiId, tenantB)
      Exp: Returns []

M2.25 reorderStages — success
      Pre: Stages A(1), B(2), C(3)
      Act: reorderStages([C.id, A.id, B.id])
      Exp: C→1, A→2, B→3

M2.26 reorderStages — missing IDs rejected
      Act: reorderStages([A.id, B.id]) when 3 stages exist
      Exp: { ok: false, message: "Ordered IDs must match..." }

M2.27 reorderStages — extra IDs rejected
      Act: reorderStages([A.id, B.id, C.id, D.id]) when 3 stages exist
      Exp: { ok: false }

M2.28 reorderStages — wrong KPI IDs rejected
      Act: reorderStages with stage IDs from different KPI
      Exp: { ok: false }

M2.29 getStageWeightSummary — correct totals
      Pre: Stages with weights 10, 25, 20, 20, 25
      Exp: totalWeight=100, stages with correct percentages (10%, 25%, 20%, 20%, 25%)

M2.30 getStageWeightSummary — all zero weights
      Pre: Stages with weights 0, 0, 0
      Exp: totalWeight=0, all percentages=0 (no division by zero)
```

---

### M3. Stage Progress Service Tests

```
M3.1  initializeStageProgress — creates records for all stages
      Pre: KPI with 5 stages, new achievement created
      Act: initializeStageProgress(achievementId, allocationId, kpiId)
      Exp: 5 KpiStageProgress records created, all isCompleted=false

M3.2  initializeStageProgress — skipped for stageless KPI
      Pre: KPI with 0 stages
      Act: initializeStageProgress
      Exp: 0 records created, no error

M3.3  initializeStageProgress — links to achievementId
      Exp: All records have correct achievementId set

M3.4  markStageComplete — happy path
      Pre: Progress record exists, user is the allocatee, period is IN_PROGRESS, no evidence required
      Act: markStageComplete(progressId, tenantId, {}, userId, "TENANT_USER")
      Exp: isCompleted=true, completedAt set, completedByUserId set

M3.5  markStageComplete — with notes
      Act: markStageComplete(progressId, tenantId, { notes: "Done writing" }, userId)
      Exp: notes stored on progress record

M3.6  markStageComplete — with evidence files
      Pre: Stage has evidenceRequired=true
      Act: markStageComplete(progressId, tenantId, { evidenceFiles: [{ name: "receipt.pdf", url: "https://...", type: "DOCUMENT" }] })
      Exp: evidenceFiles stored as JSON

M3.7  markStageComplete — evidence required but not provided
      Pre: Stage has evidenceRequired=true
      Act: markStageComplete(progressId, tenantId, {})
      Exp: { ok: false, message about evidence required }

M3.8  markStageComplete — evidence required and provided
      Pre: Stage has evidenceRequired=true
      Act: Provide valid evidenceFiles
      Exp: { ok: true }

M3.9  markStageComplete — period not IN_PROGRESS
      Pre: Period is COMPLETED or LOCKED
      Exp: { ok: false, message about period }

M3.10 markStageComplete — wrong user (not allocatee)
      Pre: Allocation assigned to user A, actor is user B (not admin)
      Exp: { ok: false }

M3.11 markStageComplete — admin can override
      Pre: Actor is TENANT_ADMIN, not the allocatee
      Exp: { ok: true } (admin bypass)

M3.12 markStageComplete — already completed (idempotent)
      Pre: Stage already marked complete
      Act: markStageComplete again
      Exp: Either success (idempotent) or error "already completed"

M3.13 markStageComplete — recalculates stageCompletionScore
      Pre: KPI has stages with weights 10, 20, 30. Stage 1 (w=10) already complete.
      Act: Complete stage 2 (w=20)
      Exp: Achievement.stageCompletionScore = 30

M3.14 markStageComplete — updates effectiveScore with credit
      Pre: Achievement has creditPercent=50, stageCompletionScore was 10
      Act: Complete another stage (w=20)
      Exp: stageCompletionScore=30, effectiveScore=15

M3.15 unmarkStageComplete — success when DRAFT
      Pre: Achievement is DRAFT, stage is completed
      Act: unmarkStageComplete
      Exp: isCompleted=false, completedAt=null, score recalculated

M3.16 unmarkStageComplete — rejected when SUBMITTED
      Pre: Achievement is SUBMITTED
      Act: unmarkStageComplete
      Exp: { ok: false, message about state }

M3.17 unmarkStageComplete — rejected when VERIFIED
      Pre: Achievement is VERIFIED
      Exp: { ok: false }

M3.18 unmarkStageComplete — recalculates score downward
      Pre: Stages w=10,20 both complete (score=30). Unmark w=20 stage.
      Exp: stageCompletionScore=10

M3.19 getStageProgressForAchievement — returns ordered
      Pre: 5 stages with progress
      Exp: Returns 5 StageProgressView items ordered by stageOrder

M3.20 getStageProgressForAchievement — includes isOverdue
      Pre: Stage deadline is 2026-01-01, today is 2026-03-22, stage not completed
      Exp: isOverdue=true

M3.21 getStageProgressForAchievement — includes daysRemaining
      Pre: Stage deadline is 2026-04-22 (31 days from now), not completed
      Exp: daysRemaining ≈ 31

M3.22 getStageProgressForAchievement — completed stage not overdue
      Pre: Stage deadline is past, but stage IS completed
      Exp: isOverdue=false

M3.23 getStageProgressForAchievement — no deadline
      Pre: Stage has no deadline
      Exp: isOverdue=false, daysRemaining=null

M3.24 getStageProgressSummary — groups by achievement
      Pre: Allocation with 2 achievements, each with 5 stages
      Exp: Returns 2 summaries, each with correct completedStages/totalStages

M3.25 calculateStageScore — sum of completed weights
      Pre: Stages w=10,20,30. Only w=10 and w=30 completed.
      Exp: stageCompletionScore=40

M3.26 calculateStageScore — zero when nothing complete
      Pre: 5 stages, none completed
      Exp: stageCompletionScore=0

M3.27 calculateStageScore — with creditPercent
      Pre: stageCompletionScore=80, creditPercent=50
      Exp: effectiveScore=40

M3.28 calculateStageScore — no creditPercent
      Pre: stageCompletionScore=80, creditPercent=null
      Exp: effectiveScore=80

M3.29 Multi-achievement isolation
      Pre: Same allocation, 2 achievements (A1, A2). Each has own stage progress.
      Act: Complete stage 1 on A1
      Exp: A1 stage 1 is complete, A2 stage 1 is still incomplete

M3.30 Weight=0 stage — trackable but no score impact
      Pre: Stage with weight=0
      Act: markStageComplete
      Exp: Success, stageCompletionScore unchanged (0 added)

M3.31 Cross-tenant isolation
      Pre: Progress record belongs to tenant A
      Act: markStageComplete from tenant B
      Exp: { ok: false }
```

---

### M4. Submission Flow Tests

```
M4.1  Submit — all stages complete, no contribution role
      Pre: KPI has 3 stages, all completed. No contributionRoles on KPI.
      Act: submitForVerification
      Exp: state=SUBMITTED, SubmissionTrail record with action="SUBMITTED"

M4.2  Submit — partial completion allowed, 2/3 stages complete
      Pre: KPI has allowPartialCompletion=true, 2 of 3 stages complete
      Act: submitForVerification
      Exp: Success, stageCompletionScore = sum of 2 completed stage weights

M4.3  Submit — partial completion NOT allowed, 2/3 stages complete
      Pre: KPI has allowPartialCompletion=false, 2 of 3 stages complete
      Act: submitForVerification
      Exp: { status: "error", message: "Complete all stages..." }

M4.4  Submit — partial allowed but 0 stages complete
      Pre: allowPartialCompletion=true, 0 of 3 stages complete
      Act: submitForVerification
      Exp: { status: "error" } — can't submit with zero progress

M4.5  Submit — KPI has no stages (R1 behavior)
      Pre: KPI with 0 stages defined
      Act: submitForVerification
      Exp: Success (R1 flow, no stage validation)

M4.6  Submit — contribution role required but missing
      Pre: KPI has contributionRoles defined, achievement has contributionRole=null
      Act: submitForVerification
      Exp: { status: "error", message: "select your contribution role" }

M4.7  Submit — contribution role provided
      Pre: KPI has roles, achievement has contributionRole="First Author"
      Act: submitForVerification
      Exp: Success, creditPercent looked up from KPI definition

M4.8  Submit — KPI evidence required but not provided
      Pre: KPI has evidenceRequired=true, achievement has no evidence
      Act: submitForVerification
      Exp: { status: "error", message: "Evidence is required" }

M4.9  Submit — period not IN_PROGRESS
      Pre: Period is COMPLETED
      Act: submitForVerification
      Exp: { status: "error" }

M4.10 Submit — creates SubmissionTrail
      Pre: Successful submission
      Exp: SubmissionTrail record with action="SUBMITTED", actorUserId, scoreAtAction

M4.11 Submit — creates notification for approver
      Pre: Successful submission
      Exp: Notification record for the approving unit head

M4.12 Submit — sets currentVerifierUnitId from source division
      Pre: KPI has no finalUnitId, no keyUnitId
      Exp: currentVerifierUnitId = startingUnitId

M4.13 Submit — sets currentVerifierUnitId from finalUnitId override
      Pre: KPI has finalUnitId set
      Exp: currentVerifierUnitId = finalUnitId

M4.14 Submit — calculates effectiveScore correctly
      Pre: Stages complete, creditPercent=50
      Exp: effectiveScore = stageCompletionScore * 0.5

M4.15 Submit — already SUBMITTED rejected
      Pre: Achievement already in SUBMITTED state
      Act: submitForVerification
      Exp: Error

M4.16 Approve — SUBMITTED → VERIFIED (single-level)
      Pre: KPI has only startingUnitId (no keyUnitId/finalUnitId)
      Act: verifyAchievement
      Exp: state=VERIFIED, SubmissionTrail action="APPROVED"

M4.17 Approve — SUBMITTED → RECOMMENDED (two-level with keyUnitId)
      Pre: KPI has keyUnitId and finalUnitId
      Act: recommendAchievement by key unit head
      Exp: state=RECOMMENDED

M4.18 Approve — RECOMMENDED → VERIFIED (final unit)
      Pre: Achievement in RECOMMENDED state
      Act: verifyAchievement by final unit head
      Exp: state=VERIFIED

M4.19 Approve — creates trail and notification
      Exp: SubmissionTrail action="APPROVED", Notification to employee

M4.20 Reject — sets state and stores reason
      Act: rejectAchievement with reason="Need receipt"
      Exp: state=REJECTED, rejectionReason="Need receipt"

M4.21 Reject — creates trail with reason
      Exp: SubmissionTrail action="REJECTED", note contains reason

M4.22 Reject — creates notification to employee
      Exp: Notification with rejection reason

M4.23 Resubmit (withdraw) — from REJECTED → DRAFT
      Pre: Achievement is REJECTED
      Act: withdrawAchievement
      Exp: state=DRAFT

M4.24 Resubmit — from SUBMITTED → DRAFT (before any action)
      Pre: Achievement is SUBMITTED, no recommendation yet
      Act: withdrawAchievement
      Exp: state=DRAFT

M4.25 Resubmit — blocked after recommendation
      Pre: Achievement has been recommended (verificationLog has RECOMMEND entry)
      Act: withdrawAchievement
      Exp: Error

M4.26 Resubmit — only reporter can withdraw
      Pre: Reporter is user A, actor is user B
      Act: withdrawAchievement
      Exp: Error

M4.27 Resubmit — employee edits stages after withdraw, resubmits
      Pre: Achievement is DRAFT after withdraw
      Act: Complete more stages → submit again
      Exp: Success, new SubmissionTrail entry

M4.28 Multiple submit/reject cycles
      Pre: Submit → Reject → Withdraw → Complete stages → Submit → Approve
      Exp: Full trail with all 5+ entries, final state=VERIFIED

M4.29 getSubmissionTrail — returns ordered history
      Pre: Achievement with 4 trail entries
      Exp: Returns array ordered by createdAt ascending

M4.30 getSubmissionTrail — cross-tenant returns empty
      Pre: Achievement belongs to tenant A
      Act: getSubmissionTrail from tenant B
      Exp: Returns []
```

---

### M5. Contribution Role Tests

```
M5.1  Create KPI with contributionRoles
      Act: createKpi({ contributionRoles: [{ role: "First Author", creditPercent: 100, isDefault: false }, { role: "Co-Author", creditPercent: 50, isDefault: true }] })
      Exp: Stored correctly, retrievable

M5.2  Update KPI — add roles
      Pre: KPI with no roles
      Act: updateKpi({ contributionRoles: [...] })
      Exp: Roles added

M5.3  Update KPI — remove roles (set null)
      Act: updateKpi({ contributionRoles: null })
      Exp: Roles removed

M5.4  Validate — duplicate role names rejected
      Act: Create KPI with roles [{ role: "Author", ... }, { role: "Author", ... }]
      Exp: Validation error

M5.5  Validate — more than one isDefault rejected
      Act: Two roles with isDefault=true
      Exp: Validation error

M5.6  Validate — creditPercent=0 rejected
      Act: Role with creditPercent=0
      Exp: Validation error (min 1)

M5.7  Validate — creditPercent=101 rejected
      Act: Role with creditPercent=101
      Exp: Validation error (max 100)

M5.8  Achievement — role selection populates creditPercent
      Pre: KPI has role "Co-Author" with creditPercent=50
      Act: Create achievement with contributionRole="Co-Author"
      Exp: creditPercent=50 on achievement

M5.9  Achievement — invalid role name rejected
      Pre: KPI has roles defined
      Act: Create achievement with contributionRole="InvalidRole"
      Exp: Error

M5.10 Achievement — no role required when KPI has no roles
      Pre: KPI has contributionRoles=null
      Act: Create/submit achievement without contributionRole
      Exp: Success, effectiveScore = stageCompletionScore (100% credit)

M5.11 Score at 100% credit
      Pre: creditPercent=100, stageCompletionScore=80
      Exp: effectiveScore=80

M5.12 Score at 50% credit
      Pre: creditPercent=50, stageCompletionScore=80
      Exp: effectiveScore=40

M5.13 Score at 30% credit
      Pre: creditPercent=30, stageCompletionScore=100
      Exp: effectiveScore=30
```

---

### M6. Notification Service Tests

```
M6.1  createNotification — stores correctly
      Act: createNotification(tenantId, userId, { type: "ACHIEVEMENT_SUBMITTED", title: "...", message: "..." })
      Exp: Record created with isRead=false

M6.2  listNotifications — ordered by createdAt desc
      Pre: 3 notifications created at t1, t2, t3
      Exp: Returns in order t3, t2, t1

M6.3  listNotifications — unreadOnly filter
      Pre: 3 notifications, 1 read
      Act: listNotifications({ unreadOnly: true })
      Exp: Returns 2

M6.4  listNotifications — pagination
      Pre: 10 notifications
      Act: listNotifications({ limit: 3, offset: 2 })
      Exp: Returns 3 notifications starting from 3rd

M6.5  markAsRead — updates isRead
      Act: markAsRead(notificationId)
      Exp: isRead=true

M6.6  markAsRead — idempotent
      Act: markAsRead on already-read notification
      Exp: No error, still isRead=true

M6.7  markAllAsRead — bulk update
      Pre: 5 unread notifications
      Act: markAllAsRead
      Exp: All 5 now isRead=true

M6.8  getUnreadCount — correct count
      Pre: 7 notifications, 3 read
      Exp: unreadCount=4

M6.9  Cross-tenant isolation
      Pre: Notification belongs to user in tenant A
      Act: listNotifications from tenant B
      Exp: Not visible

M6.10 Notification on submit
      Pre: Employee submits achievement
      Exp: Notification with type="ACHIEVEMENT_SUBMITTED" for approver

M6.11 Notification on approve
      Pre: Approver approves
      Exp: Notification with type="ACHIEVEMENT_APPROVED" for employee

M6.12 Notification on reject
      Pre: Approver rejects
      Exp: Notification with type="ACHIEVEMENT_REJECTED" for employee

M6.13 Notification on resubmit
      Pre: Employee resubmits
      Exp: Notification with type="ACHIEVEMENT_RESUBMITTED" for approver

M6.14 Multiple recipients for unit head
      Pre: Unit has 2 heads
      Act: Submit achievement
      Exp: 2 notifications created (one per head)
```

---

### M7. Dashboard Service Tests

```
M7.1  getOverviewStats — correct counts
      Pre: Period with 5 KPIs, 10 allocations, achievements in various states
      Exp: totalKpis=5, totalAllocations=10, correct achievementsByState counts

M7.2  getOverviewStats — completion percent
      Pre: 10 allocations, 3 VERIFIED
      Exp: overallCompletionPercent=30

M7.3  getOverviewStats — overdue when past period end
      Pre: Period end date is in the past, 4 allocations without VERIFIED achievement
      Exp: overdueCount=4

M7.4  getOverviewStats — no overdue when period still active
      Pre: Period end date is in the future
      Exp: overdueCount=0

M7.5  getOverviewStats — pendingReviewCount = SUBMITTED count
      Exp: pendingReviewCount matches achievementsByState.SUBMITTED

M7.6  getOverviewStats — empty period
      Pre: Period with 0 KPIs, 0 allocations
      Exp: All zeros, no errors

M7.7  getOrgHierarchyStats — returns top-level units
      Pre: No parentUnitId param
      Exp: Returns units where parentId=null

M7.8  getOrgHierarchyStats — drill-down returns children
      Pre: parentUnitId = school X
      Exp: Returns departments under school X

M7.9  getOrgHierarchyStats — correct completion percent per unit
      Pre: Unit with 5 allocations, 2 VERIFIED
      Exp: completionPercent=40

M7.10 getOrgHierarchyStats — averageScore calculation
      Pre: Unit with 3 achievements, scores 80, 60, 40
      Exp: averageScore=60

M7.11 getOrgHierarchyStats — childUnitCount for drill-down indicator
      Pre: School with 3 departments
      Exp: childUnitCount=3

M7.12 getOrgHierarchyStats — unit with zero allocations
      Pre: Unit exists but has no allocations
      Exp: totalAllocations=0, completionPercent=0, averageScore=0

M7.13 getKpiCrossComparison — correct per-unit breakdown
      Pre: KPI allocated to 3 departments with different targets/achievements
      Exp: Correct target, achieved, completionPercent per unit

M7.14 getKpiCrossComparison — handles unassigned
      Pre: Allocation without assignedToUnitId
      Exp: Listed under "Unassigned"

M7.15 getKpiCrossComparison — KPI not found
      Act: getKpiCrossComparison with invalid kpiId
      Exp: Returns null

M7.16 getKpiCrossComparison — totals
      Exp: targetTotal and achievedTotal match sum of all units

M7.17 getAttentionItems — overdue count
      Pre: Period ended, 3 allocations without submission
      Exp: overdueAchievements=3

M7.18 getAttentionItems — zero progress employees
      Pre: 5 employees with allocations, 2 have no stage progress or achievements
      Exp: zeroProgressEmployees=2

M7.19 getAttentionItems — stale pending reviews (>7 days)
      Pre: 2 achievements SUBMITTED >7 days ago, 1 submitted 2 days ago
      Exp: stalePendingReviews=2

M7.20 getAttentionItems — low completion KPIs
      Pre: KPI with 10 allocations, only 2 VERIFIED (20%)
      Exp: Appears in lowCompletionKpis with completionPercent=20

M7.21 getAttentionItems — KPI at exactly 50% not included
      Pre: KPI with 50% completion
      Exp: NOT in lowCompletionKpis (threshold is <50%)

M7.22 getStageBottleneckAnalysis — per-stage completion
      Pre: KPI with 3 stages. Stage 1: 8/10 complete. Stage 2: 4/10. Stage 3: 1/10.
      Exp: Correct completedCount and completionPercent per stage

M7.23 getStageBottleneckAnalysis — averageDaysToComplete
      Pre: Stage 1 completed by 3 users in 5, 10, 15 days
      Exp: averageDaysToComplete=10

M7.24 getStageBottleneckAnalysis — no completions
      Pre: Stage with 0 completions
      Exp: averageDaysToComplete=null

M7.25 getStageBottleneckAnalysis — KPI with no stages
      Pre: KPI has no stages defined
      Exp: stages=[]

M7.26 getPersonDetail — returns full allocation list
      Pre: User with 4 allocations across 2 KPIs
      Exp: 4 items in allocations array with correct kpiTitle, target, stages, score, state

M7.27 getPersonDetail — overallScore is sum
      Pre: User with achievements scoring 30, 50, 20
      Exp: overallScore=100

M7.28 getPersonDetail — overallCompletion
      Pre: 4 allocations, 1 VERIFIED
      Exp: overallCompletion=25

M7.29 getPersonDetail — isOverdue flag
      Pre: Allocation past period end, not VERIFIED
      Exp: isOverdue=true

M7.30 getPersonDetail — user not found
      Act: getPersonDetail with invalid userId
      Exp: Returns null

M7.31 Cross-tenant isolation on all endpoints
      Pre: Data belongs to tenant A
      Act: Query from tenant B
      Exp: Returns empty/null, not tenant A's data
```

---

### M8. API Route Tests

```
M8.1  GET /api/tenant/kra-kpi/kpis/[id]/stages — returns stages
      Act: GET request
      Exp: 200, JSON array of stages

M8.2  POST /api/tenant/kra-kpi/kpis/[id]/stages — creates stage
      Act: POST with valid body
      Exp: 201, { status: "success", data: { id } }

M8.3  POST /api/tenant/kra-kpi/kpis/[id]/stages — 403 for non-admin
      Exp: 403

M8.4  POST /api/tenant/kra-kpi/kpis/[id]/stages — 400 for invalid body
      Act: POST with empty body
      Exp: 400

M8.5  PATCH /api/tenant/kra-kpi/kpis/[id]/stages/[stageId] — updates stage
      Exp: 200, { status: "success" }

M8.6  DELETE /api/tenant/kra-kpi/kpis/[id]/stages/[stageId] — deletes stage
      Exp: 200

M8.7  DELETE — 409 when conflict
      Pre: Stage has completed progress
      Exp: 409

M8.8  POST /api/tenant/kra-kpi/kpis/[id]/stages/reorder — reorders
      Act: POST with { orderedIds: [...] }
      Exp: 200

M8.9  POST /api/tenant/kra-kpi/stage-progress/[id]/complete — marks complete
      Exp: 200

M8.10 POST /api/tenant/kra-kpi/stage-progress/[id]/complete — 400 when validation fails
      Exp: 400

M8.11 POST /api/tenant/kra-kpi/stage-progress/[id]/uncomplete — removes completion
      Exp: 200

M8.12 GET /api/tenant/kra-kpi/achievements/[id]/stage-progress — returns progress
      Exp: 200, JSON array

M8.13 GET /api/tenant/kra-kpi/achievements/[id]/trail — returns trail
      Exp: 200, JSON array

M8.14 POST /api/tenant/kra-kpi/achievements/[id]/resubmit — withdraws/resubmits
      Exp: 200

M8.15 GET /api/tenant/kra-kpi/dashboard/overview?periodId=X — returns stats
      Exp: 200

M8.16 GET /api/tenant/kra-kpi/dashboard/overview — 400 without periodId
      Exp: 400

M8.17 GET /api/tenant/kra-kpi/dashboard/org-hierarchy?periodId=X — returns units
      Exp: 200

M8.18 GET /api/tenant/kra-kpi/dashboard/org-hierarchy?periodId=X&parentUnitId=Y — drill-down
      Exp: 200

M8.19 GET /api/tenant/kra-kpi/dashboard/kpi-comparison?periodId=X&kpiId=Y — returns comparison
      Exp: 200

M8.20 GET /api/tenant/kra-kpi/dashboard/kpi-comparison — 400 without both params
      Exp: 400

M8.21 GET /api/tenant/kra-kpi/dashboard/kpi-comparison?kpiId=invalid — 404
      Exp: 404

M8.22 GET /api/tenant/kra-kpi/dashboard/attention?periodId=X — returns items
      Exp: 200

M8.23 GET /api/tenant/kra-kpi/dashboard/stage-bottleneck?periodId=X&kpiId=Y — returns analysis
      Exp: 200

M8.24 GET /api/tenant/kra-kpi/dashboard/person?periodId=X&userId=Y — returns detail
      Exp: 200

M8.25 GET /api/tenant/kra-kpi/dashboard/person?userId=invalid — 404
      Exp: 404

M8.26 All routes — 403 without auth session
      Act: Request without valid session
      Exp: 403 on every route
```

---

### M9. End-to-End Flow Tests

```
M9.1  Full happy path: Admin creates KPI with stages → Employee gets allocation → Employee completes stages → Submits → Approved → VERIFIED
      Steps:
        1. Admin creates KPI with 3 stages (w=10,20,30), contributionRoles, allowPartialCompletion=false
        2. Admin allocates to employee
        3. Employee creates achievement with title + contributionRole
        4. Stage progress auto-initialized (3 records)
        5. Employee marks stage 1 complete with notes
        6. Employee marks stage 2 complete with evidence
        7. Employee marks stage 3 complete
        8. stageCompletionScore = 60, effectiveScore = 60 * creditPercent/100
        9. Employee submits
        10. SubmissionTrail: SUBMITTED
        11. Notification to approver
        12. Approver verifies
        13. SubmissionTrail: APPROVED
        14. Notification to employee
        15. Final state: VERIFIED
      Exp: All intermediate states correct, trail has 2 entries, 2 notifications created

M9.2  Partial completion flow
      Pre: KPI with allowPartialCompletion=true, 5 stages
      Steps:
        1. Employee completes 3 of 5 stages
        2. Employee submits (partial)
        3. stageCompletionScore = sum of 3 completed weights
        4. Approved
      Exp: VERIFIED with partial score

M9.3  Reject and resubmit flow
      Steps:
        1. Employee submits with 2/3 stages
        2. Approver rejects: "Complete stage 3"
        3. Employee sees rejection notification
        4. Employee withdraws (REJECTED → DRAFT)
        5. Employee completes stage 3
        6. Employee resubmits
        7. Approver approves
      Exp: Trail has 4 entries (SUBMITTED, REJECTED, SUBMITTED, APPROVED)

M9.4  Two-level approval flow
      Pre: KPI has keyUnitId and finalUnitId
      Steps:
        1. Employee submits → SUBMITTED
        2. Key unit head recommends → RECOMMENDED
        3. Final unit head verifies → VERIFIED
      Exp: Trail has 3 entries

M9.5  Multi-target KPI (>1 achievement per allocation)
      Pre: KPI with target=3, employee has allocation
      Steps:
        1. Employee creates achievement A1 (title="Paper 1") → stages initialized
        2. Employee creates achievement A2 (title="Paper 2") → separate stages initialized
        3. Complete all stages on A1, submit → VERIFIED
        4. Complete 2/5 stages on A2, submit (partial) → VERIFIED
      Exp: Separate stage progress sets, separate scores, both tracked independently

M9.6  Stageless KPI (R1 backward compat)
      Pre: KPI with 0 stages, no contributionRoles
      Steps:
        1. Employee creates achievement (no stage init)
        2. Employee fills in actual value
        3. Employee submits
        4. Approved
      Exp: R1 flow works exactly as before, no stage-related errors

M9.7  Stage deadline overdue notification
      Pre: Stage with deadline=yesterday, not completed
      Act: Check stage progress
      Exp: isOverdue=true, daysRemaining<0

M9.8  Evidence per stage enforcement
      Pre: Stage 1: no evidence required. Stage 2: evidence required (DOCUMENT, URL).
      Steps:
        1. Complete stage 1 without evidence → success
        2. Try to complete stage 2 without evidence → error
        3. Complete stage 2 with evidence URL → success
      Exp: Correct enforcement per stage

M9.9  Dashboard reflects real-time data
      Steps:
        1. Check overview stats → 0 achievements
        2. Employee submits achievement
        3. Check overview stats → 1 SUBMITTED
        4. Approver verifies
        5. Check overview stats → 1 VERIFIED, completionPercent updated
      Exp: Dashboard numbers match actual data

M9.10 Admin manages stages after some progress
      Steps:
        1. Admin creates 3 stages
        2. Employee completes stage 1
        3. Admin tries to delete stage 1 → blocked (has completed progress)
        4. Admin updates stage 2 weight → allowed (warning but allowed)
        5. Admin adds stage 4 → allowed
        6. Admin reorders stages
      Exp: Correct behavior for each operation

M9.11 Contribution role credit cascade
      Pre: KPI with roles: First Author(100%), Co-Author(50%), Mentor(30%)
      Steps:
        1. Employee A creates achievement as "First Author" → creditPercent=100
        2. Employee B creates achievement as "Co-Author" → creditPercent=50
        3. Both complete all stages (total weight=100)
        4. Employee A effective score = 100, Employee B effective score = 50
      Exp: Different effective scores for same stage completion

M9.12 Concurrent operations
      Pre: Two users with allocations to same KPI
      Steps:
        1. User A marks stage 1 complete
        2. User B marks stage 1 complete (on THEIR progress, not A's)
        3. Both submit
      Exp: Each user has independent progress, no conflicts
```

---

### M10. Edge Cases & Boundary Tests

```
M10.1  Stage weight = 0 — valid but no score impact
M10.2  Stage weight = 0.001 — small decimal weight
M10.3  100 stages on one KPI — bulk operations
M10.4  Stage title with 200 chars (max) — accepted
M10.5  Stage title with 201 chars — rejected
M10.6  Evidence file URL with 2000 chars (max) — accepted
M10.7  20 evidence files (max array size) — accepted
M10.8  21 evidence files — rejected
M10.9  Description with 500 chars (max) — accepted
M10.10 Empty string title — rejected (min 1 char)
M10.11 Special characters in stage title (unicode, emoji) — handled
M10.12 Null deadline vs empty string deadline — both treated as no deadline
M10.13 Dashboard with 1000 allocations — doesn't timeout
M10.14 Person detail for user with 50 allocations — returns all
M10.15 Org hierarchy with 5 levels deep — drill-down works at each level
M10.16 KPI with no allocations — dashboard shows 0, no errors
M10.17 Period with no KPIs — all dashboard endpoints return empty/zeros
M10.18 Stage reorder with single stage — no-op, success
M10.19 Delete last remaining stage — KPI goes to stageless mode
M10.20 Create achievement after deleting all stages — no stage init (R1 mode)
M10.21 Submit achievement, then admin adds new stages — no impact on existing submission
M10.22 Admin changes contributionRoles after achievements exist — existing achievements keep old creditPercent
M10.23 Toggle allowPartialCompletion after some achievements submitted — no retroactive impact
M10.24 SubmissionTrail with very long notes (2000+ chars) — truncated or stored
M10.25 getStageWeightSummary with all stages having weight=0 — totalWeight=0, no division by zero
```
